{
  "name": "web-silvestra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sitio de Silvestra Paisajismo — build con prerender estatico",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:fast": "node scripts/build.mjs --skip-images",
    "serve": "node scripts/serve.mjs",
    "dev": "node scripts/build.mjs --skip-images && node scripts/serve.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "sharp": "^0.34.0"
  }
}
